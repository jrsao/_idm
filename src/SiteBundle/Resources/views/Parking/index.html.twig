{% extends 'SiteBundle:Parking:Common/layout.html.twig' %}

{% block pageTitle %}Liste des stationnements{% endblock pageTitle %}

{% block leftSide %} {{ block('indexActions') }} {% endblock leftSide %}

{% block information %}
    {% embed "CommonBundle:Listing:listing.html.twig" with {'entities': entities} %}
        
        {% block showRoute %}
{#            {{ path('parking_show', {'id': entity.id })}}#}
        {% endblock showRoute%}    
        
        {% block caption %}
        {% endblock caption%}
        
        {% block tableHeader %}
            {{ listing.header('Id')}}
            {{ listing.header('Numéro')}}
            {{ listing.header('Type')}}
            {{ listing.header('Site')}}
            {{ listing.header('Disponible')}}
        {% endblock tableHeader%}
        
        {% block tableBody %}
            {{ listing.element( entity.id )}}
            {{ listing.element( entity.no )}}
            {{ listing.element( entity.parkingType )}}
            {{ listing.element( entity.site )}}
            {{ listing.element( '-' )}}
        {% endblock tableBody%}
        
    {% endembed %}
{% endblock information%}
